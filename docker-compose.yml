version: "3.8"
services:
  baseball-mcp:
    build: .
    ports:
      - "12000:8081"
    volumes:
      - ./mlb_stats_mcp:/app/mlb_stats_mcp # Mount source code
      - ./.env:/app/.env # Mount env file too
    environment:
      - PYTHONPATH=/app
