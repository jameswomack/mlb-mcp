services:
  baseball-mcp:
    build: .
    ports:
      - "12000:8081"
    volumes:
      - ./mlb_stats_mcp:/app/mlb_stats_mcp # Mount source code
    environment:
      - PYTHONPATH=/app
    # Auto-recover when Docker Desktop / the host restarts
    restart: unless-stopped
